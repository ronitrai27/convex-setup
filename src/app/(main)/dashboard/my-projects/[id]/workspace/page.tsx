import React from "react";

const ProjectWorkspace = () => {
  return (
    <div className=" w-full p-6">
      <main>
        <h1>DEDICATED PROJECT WORKSPACE</h1>
        <p>Check here who joins and who leaves</p>
        <p>check here recent pr / fork / commit made by who ?</p>
        <p>visit to-do , time-tracker, Activity(where all commits/ pr /issue with tags created and solved, etc), Whiteboard(for team ideation , sketches and brainstorming), Code generator(from to-do/sketch to code with live preview.) and Actions ( repo visulaizer, schema visualizer, Architecture visualizer.) </p>
      </main>
    </div>
  );
};

export default ProjectWorkspace;
